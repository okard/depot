

extern crate rustnoise;
extern crate libshellng;



fn main()
{
	//parse command line arguments
	
	
	//println!("shellng");
	
	
	//run file
		// use file for lexer/parser input
		//handle output/input over stderr, stdin, stdout 
	
	//run on stdin/stdout/stderr of own process with linenoise handling
	libshellng::shell::run();
}
