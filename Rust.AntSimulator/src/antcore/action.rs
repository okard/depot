

pub enum Action {
	None,
	Move(u32, u32), 	// x,y
	Attack(u32), 		// entity id
	Work(u32),			// entity id
	Unload(u32),		// hill id
	
	//global message
	//single message
	
	//create single communication nodes
	//node message
	//subscribe node
	//unsubscribe node
	
	
}
